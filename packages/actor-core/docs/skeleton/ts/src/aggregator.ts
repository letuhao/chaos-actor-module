// Actor Core v3 — Aggregator (skeleton placeholder)
import { Aggregator } from './interfaces';
import { Snapshot } from './types';
export const AggregatorTODO: Aggregator = {
  resolve(_actor, _combiner, _caps, _layers): Snapshot {
    // TODO: implement per design Section 06 & 07
    throw new Error('TODO: Aggregator implementation');
  }
};
